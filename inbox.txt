
run at document end

store listing, 
  screenshot
  description


build script, bash wsl
  override manifest with v3, no reload script
  manifest version as argument, sed
release
reddit

icon as thumbs up emoji
